# ===================================
# Variables de Entorno - Docker
# Sistema de Boletería - TicketVue
# ===================================
#
# INSTRUCCIONES:
# 1. Copiar este archivo: cp .env.docker.example .env.docker
# 2. Configurar las variables según tu entorno
# 3. Usar con: docker-compose --env-file .env.docker up
#
# ===================================

# ===================================
# Configuración General
# ===================================
NODE_ENV=production
COMPOSE_PROJECT_NAME=ticketvue

# ===================================
# Puertos de Servicios
# ===================================
# Frontend (Nginx)
FRONTEND_PORT=80

# Backend API
BACKEND_PORT=3000

# MySQL (puedes cambiar si el puerto está en uso)
DB_PORT=3307

# ===================================
# Base de Datos MySQL
# ===================================
DB_NAME=ticketvue
DB_USER=ticketuser
DB_PASSWORD=change_this_secure_password_123
DB_ROOT_PASSWORD=change_this_root_password_456

# ===================================
# JWT Configuration
# ===================================
# Generar con: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=b86bb6243485a5067516dba6896f39b8376586739d2f1ed428cd15f4c36968ef
JWT_EXPIRE=7d

# ===================================
# Email / SMTP
# ===================================
# Opción 1: Gmail
EMAIL_SERVICE=gmail
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-password

# Opción 2: SMTP Genérico
# EMAIL_SERVICE=smtp
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=smtp-user
# SMTP_PASSWORD=smtp-password

# From email
EMAIL_FROM=Sistema TicketVue <noreply@ticketvue.com>

# ===================================
# URLs (para desarrollo local)
# ===================================
FRONTEND_URL=http://localhost
BACKEND_URL=http://localhost:3000
